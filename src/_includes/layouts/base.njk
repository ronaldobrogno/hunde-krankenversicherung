<!DOCTYPE html>
<html lang="de">
<head>
  {% include "partials/head-meta.njk" %}
  <style>
    :root {
      --primary: #0072ce; /* Barmenia-Blau */
      --secondary: #005fa3;
      --light: #f4faff;
      --dark: #333;
    }

    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      line-height: 1.6;
      background: var(--light);
      color: var(--dark);
    }

    header {
      background: var(--primary);
      color: #fff;
      padding: 1rem;
      text-align: center;
    }

    header h1 {
      margin: 0;
      font-size: 1.8rem;
    }

    nav {
      margin-top: 0.5rem;
    }

    nav a {
      color: #fff;
      margin: 0 1rem;
      text-decoration: none;
      font-weight: bold;
    }

    nav a:hover {
      text-decoration: underline;
    }

    .container {
      max-width: 1100px;
      margin: auto;
      padding: 2rem;
      background: #fff;
    }

    h2 {
      color: var(--primary);
    }

    .highlight {
      background: var(--light);
      padding: 1.5rem;
      border-left: 5px solid var(--primary);
      margin: 1.5rem 0;
    }

    .cta {
      display: inline-block;
      background: var(--primary);
      color: #fff;
      padding: 0.8rem 1.5rem;
      margin-top: 1rem;
      text-decoration: none;
      font-weight: bold;
      border-radius: 5px;
      transition: background 0.3s ease;
    }

    .cta:hover {
      background: var(--secondary);
    }

    footer {
      background: var(--primary);
      color: #fff;
      text-align: center;
      padding: 1rem;
      margin-top: 2rem;
    }
  </style>
</head>
<body>
  <header>
    <h1>{{ site.name }}</h1>
    <nav>
      <a href="/">Startseite</a>
      <a href="/leistungen/">Leistungen</a>
      <a href="/preise/">Preise</a>
      <a href="/faq/">FAQ</a>
      <a href="/kontakt/">Kontakt</a>
    </nav>
  </header>

  <main class="container">
    {{ content | safe }}
  </main>

  <footer>
    <p>&copy; {{ site.year }} {{ site.name }} – Hunde-Krankenversicherung powered by Barmenia</p>
  </footer>
</body>
</html>
